{% extends 'base.html.twig' %}

{% block content %}
    <div class="table-wrapper page-header">
        <h1><i class="material-icons md-inherit md-48">mode_edit</i>{% if delete_form is defined %}Editar{% else %}Agregar{% endif %} Liquidación</h1>
                            {% if ayuda is defined and ayuda != "" %}
                <a href="#" data-toggle="modal"  data-target="#ayudaMessage"><i class="material-icons md-inherit md-48">help</i></a>
            {% endif %}
        {% if delete_form is defined %}
            {{ form_start(delete_form, {'attr': {'class': 'text-right'}}) }}
                <input class="btn btn-danger" type="submit" value="Eliminar" />
            {{ form_end(delete_form) }}
        {% endif %}
    </div>
    {{ form_start(edit_form, {'attr': {'class': 'progress-form'}}) }}
        {{ form_widget(edit_form) }}
        <div class="col-xs-12 text-center" style="margin-top: 12px;">
            <a class="btn btn-default" href="{{ path('liquidacion_index') }}" title="Volver al listado">Cancelar</a>
            <input class="btn btn-primary" type="submit" value="Guardar" />
        </div>
    {{ form_end(edit_form) }}
{% endblock %}
