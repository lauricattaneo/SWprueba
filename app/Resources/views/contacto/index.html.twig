{% extends 'base.html.twig' %}

{% block content %}
    <h1 class="page-header">
        <i class="material-icons md-inherit md-48">email</i>
        Enviar mail: 
        {% if ayuda is defined and ayuda != "" %}
                <a href="#" data-toggle="modal"  data-target="#ayudaMessage"><i class="material-icons md-inherit md-48">help</i></a>
        {% endif %}
    </h1>    
    {{ form_start(form, {'attr': { 'class': 'progress-form'},
                                   'custom_options': {'avoid_required_message': true}}) }}
        <div class="alert alert-info alert-dismissible" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <span class="glyphicon glyphicon-info-sign" aria-hidden="true"></span>
            Los campos Nombre, Apellido, Teléfono y Email son opcionales y se incluirán en la firma del mensaje.
        </div>

        <div class="row">
            <div class="col-xs-12 col-md-6">{{ form_row(form.contactFirstName) }}</div>
            <div class="col-xs-12 col-md-6">{{ form_row(form.contactLastName) }}</div>
        </div>
        <div class="row">
            <div class="col-xs-12 col-md-6">{{ form_row(form.contactPhone) }}</div>
            <div class="col-xs-12 col-md-6">{{ form_row(form.contactMail) }}</div>
        </div>
        {{ form_row(form.destinationEmails) }}
        {{ form_row(form.subject) }}
        {{ form_row(form.message) }}
        {{ form_rest(form) }}

        <div class="col-xs-12 text-center" style="margin-top: 12px;">
            <a class="btn btn-default" href="{{ path('organismo_index') }}" title="Volver al listado">Cancelar</a>
            <input class="btn btn-primary" type="submit" value="Enviar" />
        </div>
    {{ form_end(form) }}
{% endblock %}
